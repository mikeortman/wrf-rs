# Test coverage and gap inventory

This is the test-planning ledger. The root `README.md` tracks implementation
scope; this file tracks confidence within that scope.

## Required layers

Every numerical slice should accumulate five kinds of evidence:

1. **Upstream regression parity** — port every applicable named upstream test.
2. **Differential oracle coverage** — compile the pinned Fortran routine and
   compare identical fixtures with Rust.
3. **Adversarial coverage** — add branch boundaries, malformed shapes, signed
   zero, extrema, and scientifically meaningful invariants absent upstream.
4. **Concurrency determinism** — compare one worker with multiple workers and
   exercise scheduling failures.
5. **Operational coverage** — compare full fields, restarts, I/O metadata, and
   trajectories in representative WRF cases.

## Current matrix

| Surface | Upstream parity | Added gap tests | Remaining gaps |
|---|---|---|---|
| `wrf-time` | 93/93 active `Test1.F90` cases; `ESMF_` and `WRFU_` golden outputs exact | year zero, negative year, rational normalization, invalid components | randomized long-clock sequences; leap-second policy when a caller requires it |
| CPU chunk scheduler | Not an upstream surface | disjoint writes, synchronization-confirmed concurrent workers in debug/release, typed kernel error, worker panic | nested-pool behavior; large NUMA systems; cancellation semantics |
| CPU exact-block scheduler | Not an upstream surface | exact boundaries, invalid shapes, worker panic | empty-output contract; scaling and allocation measurements |
| Registry DSL and selected generators | WRF v4.7.1 Registry executable generates five byte-identical include files and an exact eight-record metadata projection from the same fixture | physical source locations; continuations; comments and quote/case behavior; all dimension-length modes; boundary modifiers; time levels; all staggering flags; duplicate/unknown dimensions; malformed counts, ranges, quotes, and unsupported categories | includes and conditionals; typedef/package/communication forms; brace dimensions and four-dimensional scalar arrays; remaining generators; full `Registry.EM` parse |
| Domain decomposition and halo exchange | Pinned `task_for_point.c` assigns all patches exactly for three centered-remainder grids; pinned `region_bounds` plus `set_tiles2` clipping semantics match edge tiles; pinned `period.c` and `f_pack.F90` match every selected periodic destination and doubly periodic corner | signed half-open conversion, non-one origins, WRF guard-point memory bounds, invalid layouts before construction, clipped edge tiles, internal edges/corners, field staggering, bounded message buffers, complete local-vs-four-rank MPI memory equality | generated asymmetric halo descriptors; one-dimensional and larger process grids under MPI; nested/intermediate domains; multi-field message aggregation; operational model fields |
| `positive_definite_sheet` | Pinned Fortran routine compiled directly; 9 focused exact-bit cases plus 24 seeded randomized cases | epsilon boundary, signed zero, finite magnitude extremes, explicit NaN/infinity policy, invalid dimensions/totals, one-vs-four-worker determinism, statistical throughput and warmed allocation budgets | operational field distributions; trajectory integration |
| `positive_definite_slab` | Pinned Fortran routine compiled directly; focused sentinel fixture plus 16 seeded randomized cases | typed half-open region validation, varied non-one memory origins, broader domain/tile clipping, signed zero, finite magnitude extremes, explicit NaN/infinity policy, untouched storage, throughput/scaling and allocation budgets | randomized worker-count determinism; operational field distributions |
| `held_suarez_damp` | Pinned Fortran module compiled directly; 16 focused selections plus 12 seeded randomized complete-field cases | varied non-one memory origins and clipped domains, signed zero, finite magnitude extremes, explicit NaN/infinity policy, staggered-neighbor/range validation, shape mismatch before mutation, one-vs-four-worker determinism, scalar/SIMD raw-bit parity for lengths 1–257, release scaling and allocation baselines | x86-64 SIMD benchmark; complete Held-Suarez trajectory |
| column-mass staggering | Exact `calc_mu_staggered`, `calc_mu_uv`, and `calc_mu_uv_1` bodies extracted from the pinned large module; 240 non-periodic focused values, 960 big-step focused values, and 16 seeded randomized `calc_mu_staggered` complete-field cases | all physical and periodic axis combinations, split and combined mass inputs, overflow-sensitive physical endpoint expressions, missing periodic lower halos, varied non-one memory origins, clipping, signed zero, finite extremes, explicit NaN/infinity policy, validation before mutation, one-vs-four-worker determinism, untouched storage, and matched benchmark/scaling/allocation evidence | randomized big-step corpus; full ARW integration |
| `couple_momentum` | Exact pinned routine body extracted directly; six complete-field cases match all 3,780 west-east, south-north, vertical, and sentinel values by raw bits | independent and combined upper-stagger clipping, negative/non-one Fortran memory origins, half/full levels, finite overflow, zero map factors, all coefficient roles, shape/range/length validation before mutation, one-vs-four-worker determinism, matched benchmark/scaling/allocation evidence | randomized corpus; integrated `rk_step_prep` and trajectory coverage |
| `calc_ww_cp` | Exact pinned routine body extracted directly; five complete-storage cases match all 1,960 omega and sentinel values, with NaNs compared by class | interior and independent/combined upper horizontal boundaries, negative/non-one memory origins, complete physical columns, finite overflow, zero map factors, all field/coefficient roles, every range category, missing-neighbor and incomplete-column validation before mutation, one-vs-four-worker determinism, matched benchmark/scaling/allocation evidence | seeded randomized corpus; integrated `rk_step_prep` and trajectory coverage |
| `calc_cq` | Exact pinned routine body extracted directly; seven complete-storage cases match all 8,232 `cqu`, `cqv`, `cqw`, and sentinel values, with NaNs compared by class | zero, one, and three active species; ignored Registry padding slot; interior and independent/combined upper staggers; non-one/negative memory origins; vertical clipping; finite overflow; signed zero; every range/output/species failure before mutation; one-vs-four-worker determinism; matched benchmark/scaling/allocation evidence | seeded randomized corpus; Registry-generated species binding; integrated `rk_step_prep` and trajectory coverage |
| `calc_alt` | Exact pinned routine body extracted directly; six complete-storage cases match all 2,352 full inverse-density and sentinel values, with NaNs compared by class | interior and independent/combined upper-axis clipping; non-one/negative memory origins; finite overflow; cancellation; signed zero; every range and field-role failure before mutation; one-vs-four-worker determinism; matched benchmark/scaling/allocation evidence | seeded randomized corpus; integrated `rk_step_prep` and trajectory coverage |
| `calc_php` | Exact pinned routine body extracted directly; six complete-storage cases match all 2,352 pressure-point geopotential and sentinel values, with NaNs compared by class | interior and independent/combined upper-axis clipping; non-one/negative memory origins; required upper full level; source-order overflow; signed zero; opposite infinities; every range and field-role failure before mutation; one-vs-four-worker determinism; matched benchmark/scaling/allocation evidence | seeded randomized corpus; integrated `rk_step_prep` and trajectory coverage |
| `rk_step_prep` diagnostics | Exact pinned bodies for `calculate_full`, `calc_mu_uv`, `couple_momentum`, `calc_ww_cp`, `calc_cq`, `calc_alt`, and `calc_php` run in WRF order; all 1,728 stored values across twelve outputs match by raw bits | complete coupled intermediate/final fields and inactive sentinels; previously missing communicated-tile full mass; final-stage shape failure before any mutation; one-vs-four-worker determinism; matched optimized-Fortran timing and warmed allocation budget | periodic integrated case; exceptional coupled corpus; Registry state binding; short RK trajectory |
| `rk_addtend_dry` | Exact pinned routine body extracted directly; 5,616 complete RK, persistent, mass, and sentinel values match by raw bits or NaN class | first and later substeps; all upper C-grid staggers; interior vertical tile exposing `kte-1` versus `kte`; signed zero, zero map factors, infinities, shape/coefficient validation before mutation, one-vs-four-worker determinism, matched timing and allocation budget | seeded randomized corpus; Registry state binding; short RK trajectory with upstream tendency producers |
| `small_step_prep` | Exact pinned routine body extracted directly; 9,936 previous/current/saved, pressure-coefficient, omega, mass, and sentinel values match by raw bits or NaN class | first and later substeps; U/V horizontal and W/geopotential vertical staggers; interior-tile lower neighbors; complete acoustic column; exceptional arithmetic; all-field failure atomicity; one-vs-four-worker determinism; matched timing and allocation budget | seeded randomized corpus; Registry state binding; dependent acoustic coefficient/advance routines; complete small-step trajectory |
| `calc_p_rho` | Exact pinned routine body extracted directly; 3,456 inverse-density, pressure, geopotential, pressure-history, and sentinel values match by raw bits or NaN class | nonhydrostatic and hydrostatic modes; initialization and forward-damping phases; full and partial tiles; hydrostatic upward recurrence; zero denominators and infinities; all-field failure atomicity; one-vs-four-worker determinism; matched timing and allocation budget | seeded randomized corpus; Registry state binding; coupling to `small_step_prep` and `calc_coef_w`; complete acoustic trajectory |
| `calc_coef_w` | Exact pinned routine body extracted directly; 3,024 lower-diagonal, inverse-diagonal, upper-factor, and sentinel values match by raw bits or NaN class | rigid and nonrigid top boundaries; full and partial horizontal tiles; negative/non-one memory origins; complete-column behavior despite degenerate vertical tile bounds; signed zero, zero denominators, infinities; all-output failure atomicity; one-vs-four-worker determinism; matched timing and allocation budget | seeded randomized corpus; coupling to `small_step_prep`, `calc_p_rho`, and `advance_w`; complete acoustic trajectory |
| `advance_uv` | Exact pinned routine body compiled directly; all 432 U/V values in a rigid-lid nonhydrostatic case match by raw bits | governing and top-boundary modes; U/V C-grid ranges; open/symmetric/polar clipping; relaxation and periodic-X derivation; failure-before-mutation; worker determinism; matched benchmark | broader direct boundary corpus; seeded exceptional corpus; integrated acoustic trajectory |
| `advance_mu_t` | Exact pinned routine body compiled directly; all 3,168 stored omega, theta, mass, time-centered, coupled, damping, and sentinel values across four cases match by raw bits | global, specified/nested, periodic-X, and partial horizontal tiles; complete-column validation; coefficient failure atomicity; one/four-worker determinism; matched release benchmark | seeded exceptional corpus; multi-tile operational fixture; coupling to `advance_uv` and `advance_w`; complete acoustic trajectory |
| `advance_w` | Exact pinned routine body compiled directly; all 2,592 vertical momentum, perturbation geopotential, time-averaged thermodynamic, and inactive values across four cases match by raw bits | both `phi_adv_z` discretizations; rigid/nonrigid tops; enabled/disabled upper damping; terrain reconstruction; global, specified/nested, periodic-X, and partial horizontal tiles; complete-column and three-surface-level contracts; coefficient/workspace failure atomicity; zero-depth IEEE NaN propagation; one/four-worker determinism; matched release benchmark and warmed allocation accounting | seeded exceptional corpus; analytic sloping-terrain review; multi-tile operational fixture; coupled acoustic trajectory through `calc_coef_w`, `advance_uv`, and `advance_mu_t` |
| `sumflux` | Exact pinned routine body compiled directly; all 375 west-east, south-north, vertical average, halo, and sentinel values match by raw bits after three substeps | full first-step tile clearing; distinct U/V/W upper staggers; final linear recoupling; invalid phase; coefficient and field-role failure atomicity; multithreaded exactness; matched release benchmark and warmed allocation accounting | partial-tile corpus; exceptional arithmetic; coupled acoustic trajectory through every preceding small-step kernel |
| `spec_bdyupdate` | Exact pinned routine body compiled directly; all 1,728 stored values across eight mass/U/V/W-like, horizontal-mass, periodic-X, partial-tile, and inactive-tile cases match by raw bits | typed field locations replace character selectors; exact trapezoidal corner clipping; zero-width no-op; shape failure atomicity; one/four-worker determinism; matched optimized benchmark and warmed allocation accounting | seeded exceptional corpus; multi-tile operational fixture; insertion into the coupled acoustic trajectory; `spec_bdyupdate_ph` and final boundary relaxation |
| `spec_bdyupdate_ph` | Exact pinned routine body compiled directly; all 1,944 stored values across nine field-location, periodic-X, partial/inactive-tile, and exceptional cases match by raw bits or NaN class | shared exhaustively checked trapezoid geometry; source-order column-mass normalization and saved-geopotential coupling; zero-width no-op; shape/coefficient failure atomicity; one/four-worker determinism; matched optimized benchmark and allocation accounting | seeded randomized corpus; multi-tile operational fixture; insertion into the acoustic trajectory; zero-gradient and final boundary stages |
| `zero_grad_bdy` | Exact pinned routine body compiled directly; all 3,584 stored values across seven W/default/U/V, periodic-X, partial-tile, and inactive-tile cases match by raw bits | nearest-interior copies; shared trapezoid geometry; WRF's tile-lower-bound-to-domain-top vertical traversal; zero-width no-op; missing-core and shape failure atomicity; one/four-worker determinism; matched optimized benchmark and allocation accounting | seeded randomized corpus; multi-tile operational fixture; insertion after specified vertical-momentum updates; flow-dependent final relaxation |
| `flow_dep_bdy` | Exact pinned routine body compiled directly; all 3,072 stored values across six mixed-flow, periodic-X, partial south/west, partial north/east, inactive, and exceptional-sign cases match by raw bits | typed unstaggered region and U/V roles; nearest-interior outflow and positive-zero inflow; NaN, signed-zero, and infinity sign behavior; ignored upper tile bound; required upper velocity neighbors; zero-width no-op; shape/core/neighbor failure atomicity; one/four-worker determinism; matched optimized benchmark and allocation accounting | seeded randomized corpus; multi-tile moisture/TKE/scalar integration |
| `flow_dep_bdy_qnn` and `flow_dep_bdy_fixed_inflow` | Both exact pinned routine bodies compiled directly; all 3,072 stored values across six constant/preserve, periodic-X, and opposite partial-tile cases match by raw bits | one typed inflow policy shared with the base traversal; finite, negative-zero, and infinite constants; preserve-current inflow; ignored upper tile bound; one/four-worker determinism; matched optimized benchmarks and allocation accounting | seeded randomized corpus; operational CCN initialization coverage; integration with scalar selection logic |
| Complete local acoustic trajectory | Seven exact pinned routine bodies execute in `solve_em.F` order; all 2,196 selected final prognostic, pressure, inverse-density, and averaged-flux values match after three substeps by raw bits | shared typed controls and role groups; explicit `fnm`/`fnp` mapping; all-stage preflight; final-stage shape failure leaves every mutable field unchanged; one/four-worker complete-state determinism | periodic, polar, specified/nested, and hydrostatic trajectories; communication and physical-boundary insertion; Registry state binding; coupled large-step trajectory and restart |
| Kessler microphysics | Pinned `module_mp_kessler.F` compiled directly; all 660 mutable field, halo, and precipitation values match by raw bits | dry/moist columns, four rain regimes, cloud threshold branches, multi-step sedimentation, non-one Fortran horizontal origins, typed parameter/range/shape failures before mutation, one-vs-four-worker determinism, untouched halos, matched benchmark and allocation budget | microphysics driver and Registry species mapping; exceptional-value policy; randomized corpus; coupled precipitation trajectory and restart |
| Minimum NetCDF/restart schema | Independent NetCDF-C and Rust writers produce byte-identical classic 64-bit-offset files plus exactly equal ordered schema, metadata, and field bits for 13 ARW variables | invalid timestamp/dimension/name/spacing; missing, unexpected, duplicate, wrong-type, and wrong-length data; float-attribute raw-bit equality; bounded chunk coverage; exact first differing restart element | complete Registry-selected fields/dimensions; restart alarm state; multiple records; NetCDF-4 write/chunk/compression policy; restart a pinned WRF trajectory from each implementation |

## Fixture policy

Golden files are generated from a named pinned upstream build, never from the
Rust implementation. Floating-point fixtures store raw IEEE-754 bits where
exact parity is expected. A tolerance is allowed only when the responsible
algorithm page documents why exact ordering cannot or should not be retained,
and the test reports absolute, relative, and ULP error separately.

The seeded ARW corpora contain 68 cases and 39,588 complete output values.
Finite values and infinities compare by raw bits. NaN outputs compare by class,
because payload and sign propagation are not portable model contracts. Every
failure reports the seed, field, and first divergent linear index. The oracle
regenerates and byte-compares the committed inputs before compiling Fortran, so
generator drift cannot silently rewrite the test population.

Registry goldens under `parity/registry/golden` are generated by WRF's own
`tools/registry` executable. The Rust unit test compares every byte, while
`scripts/run-registry-oracle.sh` independently regenerates both WRF and Rust
outputs and compares both sides to the committed source-of-truth files.
