[package]
name = "wrf-time"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "WRF-compatible model time and rational interval primitives"

[lints]
workspace = true

[lib]
name = "wrf_time"
path = "src/lib.rs"
